import React, { useEffect } from "react";

const Timer: React.FC = () => {
  // const [time, setTime] = useState("00:00:00");

  useEffect(() => {
    // Timer logic here
  }, []);

  return <span id="watch">{}</span>;
};

export default Timer;
